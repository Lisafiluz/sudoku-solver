package Search;

import java.util.Collection;

public class SudokuSearch {
	
	public static Collection<Integer[][]> getNextStates(Integer[][] state, int row, int col) {
		int startRow = (row / 3) * 3;
		int startCol = (col / 3) * 3;
		return null;
	}
}
